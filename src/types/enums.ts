export enum RepeatType {
    Weekly = "Weekly",
    Monthly = "Monthly",
    Daily = "Daily",
}

// Define the Status enum
export enum Status {
    Pending = "Pending",
    Completed = "Completed",
    InProgress = "In Progress",
    Reopen = "Reopen",
}