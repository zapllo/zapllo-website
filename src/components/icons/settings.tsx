import clsx from 'clsx'
import React from 'react'

type Props = { selected: boolean }

const Settings = ({ selected }: Props) => {
  return (
    <img src='/icons/setting.png' className='h-3'/>
   
  )
}

export default Settings
